name: Publish

on:
  workflow_dispatch:
  push:
    tags:
      - 'v*'

jobs:
  publish:
    runs-on: ubuntu-latest
    permissions:
      id-token: write
      contents: read

    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-dotnet@v4
        with:
          dotnet-version: '10.0.x'

      - name: Pack
        run: |
          chmod +x ./build.sh
          ./build.sh package

      - name: NuGet login (OIDC)
        uses: NuGet/login@v1
        id: login
        with:
          user: AZander48

      - name: Push
        run: |
          shopt -s nullglob
          packages=(bin/*.nupkg)
          if [ ${#packages[@]} -eq 0 ]; then
            echo "No .nupkg files found in bin/"
            exit 1
          fi
          for package in "${packages[@]}"; do
            dotnet nuget push "$package" \
              --skip-duplicate \
              --api-key "${{ steps.login.outputs.NUGET_API_KEY }}" \
              --source https://api.nuget.org/v3/index.json
          done
